<% root = assemblies.shift %>
<%= link_to translated_attribute(root.title), assembly_path(root), class: "button button__text-secondary" %>

<% assemblies.each do |assembly| %>
  <%= icon "arrow-right-line" %>
  <%= link_to translated_attribute(assembly.title), assembly_path(assembly), class: "button button__text-secondary" %>
<% end %>
