<h2 class="h5 md:h3 decorator" role="status" aria-atomic="true"><%= t("assemblies.order_by_assemblies.assemblies", scope: "layouts.decidim", count: collection.total_count) %></h2>

<div class="card__grid-grid">
  <%= render(collection) %>
</div>

<%= decidim_paginate collection %>
