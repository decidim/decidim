<% form.object.election.questions.each do |question| %>
  <%= content_tag :h5,
              translated_attribute(question.title),
              class: "heading5 mt-sm" %>

  <div class="card--secondary">
    <div class="card__content">
      <%= render partial: "answer_results_form_fields", locals: { form: form, question: question } %>

      <% if question.min_selections == 0 %>
        <%= form.fields_for "[question_results][]", question do |question_form| %>
          <%= question_form.hidden_field :id %>

          <%= question_form.number_field(:votes_count,
                                   value: question.results.blank_question.find_by(polling_station: polling_station)&.votes_count.to_i,
                                   label: t("decidim.elections.votes.confirm.nota_option"),
                                   class: "medium-3") %>
        <% end %>
      <% end %>
    </div>
  </div>
<% end %>
