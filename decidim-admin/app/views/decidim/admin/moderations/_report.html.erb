<% if report.details.blank? %>
  <%= t(".reasons.#{report.reason}") %>
<% else %>
  <span title="<%= report.details&.truncate(250) %>">
      <%= with_tooltip(t(".reasons.#{report.reason}")) { t(".reasons.#{report.reason}") } %>
  </span>
<% end %>
