<%= render layout: "decidim/shared/filters", locals: { filter_sections: filter_sections(**filter_options), search_variable:, skip_to_id: "meetings" } do %>
  <%= render partial: "decidim/meetings/calendar_modal", locals: {
        path: short_url(
          route_name: "calendar",
          params: { filter: params.fetch(:filter, {}).try(:to_unsafe_hash) }
        )
      } %>
<% end %>
