<h3><%= t ".title" %></h3>

<%= form_for(@form, url: meeting_meeting_close_path(meeting, @form)) do |f| %>
  <%= render partial: 'form', object: f %>

  <div class="actions">
    <%= f.submit t(".close") %>
  </div>
<% end %>
