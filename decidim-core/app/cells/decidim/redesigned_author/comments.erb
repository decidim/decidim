<% if commentable? %>
  <%= link_to "#{resource_locator(from_context).path}#comments", title: t("decidim.author.comments", count: from_context.comments_count), class: "flex items-center gap-1" do %>
    <%= icon "chat-1-line", class: "text-gray fill-current flex-none" %>
    <%= from_context.comments_count %> <%= t("decidim.author.comments", count: from_context.comments_count) %>
  <% end %>
<% end %>
