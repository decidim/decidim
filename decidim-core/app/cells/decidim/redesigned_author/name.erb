<% data = !disable_tooltip && has_tooltip? ? { tooltip: render(:profile_minicard).html_safe } : nil %>
<% if profile_path? %>
  <%= link_to display_name, profile_path, class: "author__name", data: %>
<% else %>
  <%= content_tag :span, display_name, class: "author__name", data: %>
<% end %>
