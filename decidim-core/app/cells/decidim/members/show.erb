<div class="empty-notifications callout secondary <%= "hide" if members.any? %>">
  <p><%= t("decidim.members.no_members") %></p>
</div>
<div class="row small-up-1 medium-up-2 card-grid">
  <% members.each do |member| %>
    <%= card_for member %>
  <% end %>
</div>
<%= decidim_paginate members %>
