<% sections_to_render.each_with_index do |(type, results), index| %>
  <%= cell(
    "decidim/search_results_section",
    { type => results },
    is_paginated: has_selected_resource_type?
  ) %>
<% end %>
