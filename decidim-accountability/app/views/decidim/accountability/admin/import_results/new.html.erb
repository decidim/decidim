<%= form_tag(import_results_path, multipart: true, class: "form new_import") do %>
  <div class="card">
    <div class="card-divider">
      <h2 class="card-title">
        <%= t(".title") %>
      </h2>
    </div>

    <div class="card-section">
      <div class="row column">
        <%= file_field_tag :csv_file %>
      </div>
    </div>
  </div>

  <div class="button--double form-general-submit">
    <%= submit_tag t(".import"), class: "button" %>
  </div>
  
  <%# it would be convenient to introduce an explanation of how the import file should be, 
  as well as how this import process occurs with each record. (Ex: what happens to records 
  with identifiers that already exist, etc.) %>
<% end %>
