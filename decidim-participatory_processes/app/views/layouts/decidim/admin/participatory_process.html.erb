<% add_decidim_page_title(translated_attribute(current_participatory_space.title)) %>

<% content_for :breadcrumb_nav do %>
<div class="process-title">
  <div class="container">
    <div class="process-title-content">
      <div class="process-title-content-breadcrumb-container-left">
        <%= link_to decidim_admin.root_path, class: "process-title-content-icon-home", data: { "external-link": false } do %>
          <%= icon "home-gear-line" %>
        <% end %>
        <span class="text-white">/</span>
        <%= link_to t("decidim.admin.titles.participatory_processes"), decidim_admin_participatory_processes.participatory_processes_path, data: { "external-link": false }, class: "process-title-content-breadcrumb-no-active" %>
        <span class="text-white">/</span>
        <span class="process-title-content-breadcrumb">
          <%= translated_attribute(current_participatory_space.title) %>
        </span>
      </div>
      <div class="process-title-content-breadcrumb-container-right">
        <%= link_to decidim_participatory_processes.participatory_process_path(current_participatory_space), class: "process-title-content-breadcrumb-container-right-link" do %>
          <%= icon "eye-line" %>
          <span>
            <%= t("see_process", scope: "decidim.admin.actions") %>
          </span>
        <% end %>
        <div class="inline-block relative">
          <%= button_tag id: "processes-dropdown-menu-trigger", data: { component: 'dropdown', target: 'processes-dropdown-menu-settings' }, class: 'dropdown__trigger button button__transparent' do %>
            <%= icon "more-line" %>
          <% end %>
          <div id="processes-dropdown-menu-settings" class="process-title-content-dropdown">
            <%= sidebar_menu(:admin_participatory_process_menu).render %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>

<%= render "layouts/decidim/admin/application" do %>
  <div class="process-content">
    <%= yield %>
  </div>
<% end %>
