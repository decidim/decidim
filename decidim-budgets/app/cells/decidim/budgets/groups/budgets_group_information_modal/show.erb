<button class="link" data-open="budget-modal-info" aria-controls="budget-modal-info" aria-haspopup="true" tabindex="0"><%= t(".more_information") %></button>
<div class="reveal" data-reveal id="budget-modal-info">
  <div class="reveal__header">
    <h3 class="reveal__title"><%= decidim_sanitize(title) %></h3>
    <button class="close-button" data-close aria-label="Close modal" type="button">
      <span aria-hidden="true">×</span>
    </button>
  </div>
  <p><%= decidim_sanitize(description) %></p>

  <% if discardable.any? %>
    <div class="callout alert">
      <%= t(".limit_reached") %>
      <ul>
      <% discardable.each do |budget| %>
        <li><%= link_to(t(".limit_reached_cancel", name: translated_attribute(budget.name)), order_path_for(budget), method: :delete, class: "cancel-order", data: { confirm: t("projects.budget_summary.are_you_sure", scope: "decidim.budgets") }) %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= highlighted_heading if highlighted %>
  </p>
  <p>
    <%= link_to t(".back_to", group_name: group_name), main_component_path(group_component) %>
  </p>
  <div class="row">
    <div class="columns medium-8 medium-offset-2">
      <button class="button expanded" data-close><%= t(".continue") %></button>
    </div>
  </div>
</div>
