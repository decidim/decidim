<div class="card">
  <div class="card-divider">
    <h2 class="card-title"><%= title %></h2>
  </div>

  <div class="card-section">
    <div class="row column">
      <%= form.text_field :title %>
    </div>

    <div class="row column">
      <%= form.text_area :body, rows: 10 %>
    </div>

    <% if feature_settings.geocoding_enabled? %>
      <div class="row column">
        <%= form.text_field :address %>
      </div>
    <% end %>

    <% if @form.categories&.any? %>
      <div class="row column">
        <%= form.categories_select :category_id, @form.categories, prompt: t(".select_a_category") %>
      </div>
    <% end %>

    <% if current_participatory_process.subscopes.any? %>
      <div class="row column">
        <%= form.collection_select :scope_id, participatory_process_subscopes(current_participatory_process), :id, :name %>
      </div>
    <% end %>
  </div>
</div>
