<%
js_configs = {
  icons_path: image_path("decidim/icons.svg")
}
%>

<!-- script>
  // TODO-blat: import the image into javascript using import, and get the path to pass it to `.set`
  Decidim.config.set(<%== js_configs.to_json %>);
</scrip--t>
